<style>

.box{margin-bottom:10px;}
.box .box-hd{position:relative;height:27px;padding:0 10px;border:1px solid #bad6ff;border-bottom:none;background:#e5efff url(bg-repeatx.png) repeat-x 0 -10px;}
.box .box-hd i{float:left;width:16px;height:16px;margin:5px 4px 0 0;}
.box .box-hd h3{float:left;line-height:26px;}
.box .box-act{float:right;margin-top:5px;}
.box .box-act a{float:right;margin-left:6px;line-height:16px;}
.box .box-bd{padding:10px;border:1px solid #bad6ff;background:#D9EBF5;height:140px;}

.box .box-tp,
.box .box-bt{display:block;position:relative;height:4px;overflow:hidden;margin:0 0 -4px 0;z-index:10;}
.box .box-bt{margin:-4px 0 0 0;}
.box .box-tp b,
.box .box-bt b{display:block;float:right;width:4px;height:4px;overflow:hidden;}
.box .box-tp,
.box .box-tp b,
.box .box-bt,
.box .box-bt b{background:url(bg-repeatx.png) no-repeat -3px 0;}
.box .box-tp b{background-position:0 0;}
.box .box-bt{background-position:-3px -3px;}
.box .box-bt b{background-position:0 -3px;}


body{
    padding:10px;
}
      .container {
        border: 1px solid #366EBE;
        height: 250px;
        padding: 10px;
        position: relative;
        overflow: auto;
    }

    .component {
        margin: 10px 10px;
        width: 200px;
        height: 180px;
        display: inline-block;
        *display: inline;
        *zoom: 1;
        overflow: hidden;
    }

    .cheader {
        cursor: move;
        border: 1px solid black;
        height: 20px;
        line-height: 20px;
    }

    .component .cheader {
        cursor: move;
    }

    .ks-dd-dragging {
        border: 2px #4886DE dashed;
    }

     .ks-dd-dragging * {
                visibility: hidden;
            }

</style>



  <div class="effect-list">
     <ul>
        <li>向左</li>
        <li>向右</li>
        <li>向上</li>
        <li>向下</li>
     </ul>
  </div>

<!--<div id="container2" class="container">


    <div class="box component">
        <s class="box-tp"><b></b></s>

        <div class="box-hd cheader">
            <h3>拖动头</h3>
        </div>
        <div class="box-bd">
            drag proxy 1
        </div>
        <s class="box-bt"><b></b></s>
    </div>
    <div class="box component">
        <s class="box-tp"><b></b></s>

        <div class="box-hd cheader">
            <h3>拖动头</h3>
        </div>
        <div class="box-bd">
            drag proxy 2
        </div>
        <s class="box-bt"><b></b></s>
    </div>
    <div class="box component">
        <s class="box-tp"><b></b></s>

        <div class="box-hd cheader">
            <h3>拖动头</h3>
        </div>
        <div class="box-bd">
            drag proxy 3
        </div>
        <s class="box-bt"><b></b></s>
    </div>
</div>-->

<script>
   /*KISSY.use("node,dd,dd/plugin/scroll,dd/plugin/proxy", function (S, Node, DD, Scroll, Proxy) {

        var DDM = DD.DDM,
                DraggableDelegate = DD.DraggableDelegate,
                DroppableDelegate = DD.DroppableDelegate,
                Draggable = DD.Draggable,
                Droppable = DD.Droppable;


        S.ready(function () {

            var dragDelegate = new DraggableDelegate({
                container: "#container2",
                handlers: ['.cheader'],
                selector: '.component',
                move: true,
                plugins: [
                    new Proxy({

                        node: function (drag) {
                            var n = S.one(drag.get("dragNode")[0].cloneNode());
                            n.removeAttr('id');
                            n.css("opacity", 0.8);
                            return n;
                        },
                        // 主体位置不跟随 proxy
                        moveOnEnd: false,
                        // 每次 proxy 都重新生成
                        destroyOnEnd: true
                    }),
                    new Scroll({
                        node: "#container2"
                    })
                ]
            });

            var dropDelegate = new DroppableDelegate({
                container: "#container2",
                selector: '.component'
            });

            dragDelegate.on("dragover", function (ev) {
                var drag = ev.drag;
                var drop = ev.drop;
                var dragNode = drag.get("dragNode"),
                        dropNode = drop.get("node");
                var middleDropX = (dropNode.offset().left * 2 + dropNode.width()) / 2;
                if (ev.pageX > middleDropX) {
                    var next = dropNode.next();
                    if (next && next[0] == dragNode[0]) {

                    } else {
                        dragNode.insertAfter(dropNode);
                    }
                } else {
                    var prev = dropNode.prev();
                    if (prev && prev[0] == dragNode[0]) {
                    } else {
                        dragNode.insertBefore(dropNode);
                    }
                }
            });

            var container = S.one("#container2");
            var id = 4;
            container.unselectable();

            var newTpl= S.one('#newTpl').html();
            S.one("#add").on("click", function () {
                new Node(S.substitute(newTpl,{
                    id:(id++)
                })).appendTo(container).unselectable();
            });
        });
    }); */
</script>

<style>
html, body {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
}

ul, ol {
    -webkit-margin-before: 0;
    -webkit-margin-after: 0;
    -webkit-margin-start: 0;
    -webkit-margin-end: 0;
    -webkit-padding-before: 0;
    -webkit-padding-after: 0;
    -webkit-padding-start: 0;
    -webkit-padding-end: 0;
}

body * {
    -webkit-text-size-adjust:none;
    /*-webkit-user-select: none;*/
    -webkit-tap-highlight-color: rgba(0,0,0,0);
}

body > div {
	width:100%;
	height:100%;
	overflow:hidden;
	position:absolute;
	top:0;
	left:0;
}

.viewport {
	position:relative;
	width:100%;
	height:100%;
	box-sizing:border-box;
	padding-bottom:100px;
}

.h{
  width:300px;
  height:300px;
  background:red;
}
.y{
  width:300px;
  height:300px;
  background:yellow;
}



</style>
    <div class="viewport">
		<div class="active"></div>
		<div class="inactive"></div>
	</div>
<script>


var viewportEl = $('.viewport');
var activeEl = $('.viewport .active');
var inactiveEl = $('.viewport .inactive');

activeEl.html('<div class="h"><img src=""/><div class="overlay"></div></div>');
inactiveEl.html('<div class="y"><div class="overlay"></div></div>');

var types = ['LI', 'LO', 'RI', 'RO', 'TI', 'TO', 'BI', 'BO'];


app.module.Transition.float(activeEl[0], 'LI', 10, function() {

});

</script>
